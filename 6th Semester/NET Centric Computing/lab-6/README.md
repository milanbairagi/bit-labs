# Lab 6

This is a small ASP.NET Core MVC project that uses SQLite and performs basic CRUD operations for students.

## Commands

Trust the HTTPS development certificate:
```bash
dotnet dev-certs https --trust
```

Update database:
```bash
dotnet ef database update
```

Run the app:
```bash
dotnet run --launch-profile https
```
